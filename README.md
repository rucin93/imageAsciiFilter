# imageAsciiFilter

### Usage:
```js
new asciiFilter({
    element: document.getElementById('source here'),
    container: document.getElementById('result here'),
    width: 63,
    color: true,
    callback: asciiString => {
        // do something with asciiString here
    }
})
```